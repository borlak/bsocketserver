#ifndef __TYPEDEFS_H
#define __TYPEDEFS_H

typedef struct buffer_list BUFFER_LIST;
typedef struct connection CONNECTION;
typedef struct profile PROFILE;
typedef struct smart_buf SMART_BUF;

#endif // __TYPEDEFS_H
